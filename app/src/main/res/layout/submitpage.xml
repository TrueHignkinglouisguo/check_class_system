<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/signin_background"
    >
    <RelativeLayout
        android:id="@+id/submitpage_info_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <include
            android:id="@+id/submitpage_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            layout="@layout/submitpage_show"
            />
        <include
            android:id="@+id/submitpage_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="gone"
            layout="@layout/loginpage_progress"/>
    </RelativeLayout>
    <TextView
        android:id="@+id/submitpage_submit"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/submitpage_info_layout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:background="@drawable/homepage_submitbutton"
        android:text="确认提交"
        />
    <TextView
        android:id="@+id/submitpage_cancel"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/submitpage_submit"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:background="@drawable/submitpage_cancelbutton"
        android:text="取消"
        />
</android.support.constraint.ConstraintLayout>